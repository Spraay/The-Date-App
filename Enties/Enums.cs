namespace Entity
{
    public enum Gender
    {
        None, Male, Female
    }
    public enum Eyes
    {
        None, Brown, Green, Blue, Gray
    }
    public enum Status
    {
        Pending,
        Accepted,
        Declined,
        Blocked
    }
}
