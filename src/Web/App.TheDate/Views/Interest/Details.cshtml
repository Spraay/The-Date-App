@model Interest

@{
    ViewData["Title"] = "Details";
}

<br />
<br />
<div class="container">
    <div class="row">
        <div class="col s12 m12 12">
            <div class="card-panel">
                <h4 class="header2">Interest Details</h4>
                <div class="row">
                    <label>@Html.DisplayNameFor(model => model.Name)</label><p>@Html.DisplayFor(model => model.Name)</p>

                </div>
            </div>
        </div>
    </div>

    <div>
        <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
        <a asp-action="Index">Back to List</a>
    </div>
</div>

